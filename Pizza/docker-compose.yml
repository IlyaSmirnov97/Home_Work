version: "3.1"

volumes:
  pg_crud:

services:
  PizzaDB:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=PizzaDB
    volumes:
      - pg_crud:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - my_network

  app:
    restart: always
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - PizzaDB
    environment:
      SPRING_DATASOURCE_URL: 'jdbc:postgresql://PizzaDB:5432/PizzaDB' 
      JAVA_OPTS: '-Xmx512m'
    networks:
      - my_network

networks:
  my_network:
    driver: bridge